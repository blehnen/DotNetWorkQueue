name: Claude PR review

# A second opinion on pull requests, alongside CodeRabbit. It is deliberately a
# supplement rather than a replacement: most changes here are authored with Claude,
# so this reviewer shares the author's priors even though it does not share the
# authoring session's context. A differently-built reviewer catches things it will
# not, and the reverse is also true.
#
# Authentication is a Claude subscription via CLAUDE_CODE_OAUTH_TOKEN, generated
# with `claude setup-token`.
#
# Fork pull requests are skipped explicitly. GitHub does not pass repository
# secrets to them, so the action would run, find no token and fail on every such
# pull request - a red check that says nothing about the change. The condition on
# the job turns that into not running at all. Dependabot is skipped for the same
# reason: its pull requests read from a separate secret store, so this one is not
# available to them either, and dependency bumps are not what this is for.
#
# Note this differs from the other workflows here, which do run for fork pull
# requests; they need no secret to be useful, and they run on GitHub's throwaway
# runners rather than on our own hardware.
#
# Both actions are pinned to commit SHAs rather than moving tags. This job holds a
# long-lived credential and can mint an OIDC token, so a retagged or compromised
# upstream release would run with both. Dependabot can propose SHA bumps for review.

# DISABLED on pull requests, pending an upstream fix. Every run that reached a model
# died on its first request - one turn, under two seconds, empty modelUsage,
# is_error:true - and the action reports that as a plain failure with no message.
#
# This is a known and open upstream problem, not a fault in this configuration:
#   anthropics/claude-code-action#1614 - the same envelope, with the real cause
#     (a 401 on the OAuth token, or a 429 for some reporters) visible only under
#     show_full_output. Reported on both Pro and Max, and reproduced on a pinned
#     known-good action revision, so it is server side rather than a regression.
#   anthropics/claude-code-action#1720 - the same envelope across many action
#     versions and with API key auth as well as OAuth.
#
# Re-minting the token does not help: reporters there went through four tokens for
# the same 401, and we saw the identical failure on two.
#
# Left runnable by hand so the fix can be tested without reopening it to every pull
# request. To restore, put back the pull_request trigger below.
on:
  workflow_dispatch:
  # pull_request:
  #   types: [opened, synchronize, reopened, ready_for_review]

jobs:
  review:
    # Same-repository, human-authored pull requests only - see the note above. The
    # first clause is skipped under workflow_dispatch, which carries no pull request
    # payload and can only be started by someone with write access anyway.
    if: >-
      (github.event_name == 'workflow_dispatch' ||
       github.event.pull_request.head.repo.full_name == github.repository) &&
      github.actor != 'dependabot[bot]'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      id-token: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          fetch-depth: 1

      - name: Claude PR review
        uses: anthropics/claude-code-action@3b8197d3d486006dd4af54613517f21ac6ac625e # v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          prompt: |
            REPO: ${{ github.repository }}
            PR NUMBER: ${{ github.event.pull_request.number }}

            Review this pull request. The branch is already checked out.

            Read CLAUDE.md first and treat it as authoritative. This repository has
            deliberate conventions that look like defects if you do not know them,
            and reporting one as a finding wastes a maintainer's time. Among them:

            - Integration test projects target net10.0 only, on purpose.
            - TreatWarningsAsErrors and XML documentation are enabled for the shipped
              library projects and deliberately not for test projects.
            - Test projects carry no licence header by design; do not ask for one.
            - Assertions are MSTest. FluentAssertions was removed on licensing
              grounds and must not be suggested.

            If something looks wrong, check whether it is established practice in the
            surrounding code before reporting it. Say so plainly when you are unsure
            rather than asserting it.

            Focus on what a careful reviewer would actually catch:

            - Correctness, especially edge cases and error handling
            - Concurrency and async: unawaited work, unobserved exceptions, races
            - Resources that can leak on a failure path
            - Tests that cannot fail, or that pass for a reason other than the one
              they claim to check
            - Security implications of the change

            Report findings as inline comments on the lines they concern, with one
            short top-level summary. Report nothing if the change is sound - silence
            is a useful signal and padding is not.

          claude_args: |
            --allowedTools "mcp__github_inline_comment__create_inline_comment,Bash(gh pr comment:*),Bash(gh pr diff:*),Bash(gh pr view:*)"

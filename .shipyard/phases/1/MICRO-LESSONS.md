- [PLAN-1.1 Task 1] `NetMQQueue<T>` + `NetMQPoller` + `ReceiveReady` all work on NetMQ 4.0.2.2; no PairSocket fallback needed.
- [PLAN-1.1 Task 2] `internal` + `InternalsVisibleTo` (main csproj line 32) gives the Tests project free access to `SetCountMsg` with zero friction — no need to make it `public`.
- [PLAN-1.1] Subagent Write tool was blocked from writing into `.shipyard/phases/1/results/` — orchestrator now writes SUMMARY files itself after each builder returns. Future waves: include the SUMMARY directly in the builder prompt asking for inline report.
- [PLAN-1.2 Task 2] `NetMQActor.ReceiveReady` is typed `EventHandler<NetMQActorEventArgs>`, NOT `NetMQSocketEventArgs`. Callback signature must be `(object sender, NetMQActorEventArgs e)`. Relevant for any future poller wiring touching the actor.
- [PLAN-1.2 Task 2] Intermediate state (caller-thread writes to `_actor` while poller owns reads) did NOT deadlock in tests — the lock + brief window worked fine. Future refactors don't need to panic about this pattern for simple cases.
- [PLAN-1.2 Task 3] `_lockSocket` removed cleanly in one task (9 → 0) by deleting every `lock(_lockSocket)` block at once, including `Dispose`'s wrapper. No need to phase the removal.
- [PLAN-1.3 Task 1] **Plan-text bug caught in review**: architect's code snippet used `TaskSchedulerBusCommands.AddedNode` for the initial publish, but the pre-refactor code used `BroadCast`. These are different wire messages with different handlers (`BroadCast` triggers welcome replies; `AddedNode` just logs). Builder correctly followed plan verbatim; orchestrator caught the regression post-build and reverted (commit `fda0fd4`). Lesson for architect: when a refactor preserves existing wire-protocol semantics, the plan must quote the original command enums from the source file, not paraphrase them.
- [PLAN-1.3 Task 2] `TaskSchedulerMultiple.Start()` was the only production caller of `_jobCount.Start()` and already wrapped it in `Task.Run`. Making `Start()` non-blocking and dropping the wrapper was a net zero-behavioral-change cleanup for that single call site.
- [PLAN-1.3 Task 2] Removing `Task.Run` also required removing the now-unused `using System.Threading.Tasks;` import — `TreatWarningsAsErrors=true` in Release would have otherwise flagged it. Watch for stranded usings after any `Task`-related cleanup.
- [PLAN-2.1 all tasks] **Repeat of the PLAN-1.3 plan-bug pattern**: architect's code snippets called `new TaskSchedulerBus(logger, port, beaconInterface)`, but the actual constructor is `(logger, TaskSchedulerMultipleConfiguration)`. Every new test file would have failed to compile. Builder correctly pre-flighted by reading the existing test file + `TaskSchedulerBus.cs` and fixed the snippets during Task 1. Architect lesson: when embedding inline code that references constructors, always cross-reference the actual source file; don't extrapolate from method names. This is now a recurring pattern across Phase 1 and deserves a first-class entry in `.shipyard/LESSONS.md`.
- [PLAN-2.1 Task 1] Concurrency test under the new lock-free model runs in ~3s (12 threads × 5000 iterations each). Prior lock-contended implementation was materially slower under the same shape — the 30s deadlock-detector deadline was a generous safety margin; actual runs are 10x under it. Fix achieves the expected throughput improvement.
- [PLAN-2.1 Task 2] Two-node NetMQ tests in this repo use a **single shared port** for all nodes on the bus. The beacon discovery layer handles node addresses separately. Don't assume two nodes need different bus ports.
- [PLAN-2.1 Task 3] New tests call `sync.Start()` directly (no `Task.Run` wrapper) and still complete in ~3s, confirming PLAN-1.3's non-blocking-Start contract works end-to-end.
- [PLAN-2.1 process] `git log ... | head -N` in WSL drvfs may yield exit code 1 via SIGPIPE without being an error — prefer `git log -n N` directly.
